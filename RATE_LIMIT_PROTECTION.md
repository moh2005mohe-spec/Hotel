# حماية Rate Limit - Rate Limit Protection

## ما هو Rate Limit؟

**Rate Limit** هو حد أقصى لعدد الرسائل البريدية التي يمكن إرسالها من نفس الحساب في فترة زمنية قصيرة.

### المشكلة التي حدثت:
```
خطأ: "email rate limit exceeded"
```

هذا يعني أن عدد محاولات إرسال البريد تجاوز الحد المسموح.

---

## الحل المُطبّق

تم إضافة **حماية تلقائية** في الواجهة الأمامية:

### 1. الكشف عن الخطأ
```typescript
if (error.includes('rate limit') || error.includes('too many')) {
  // تفعيل الحماية
}
```

### 2. تعطيل الزر لمدة 60 ثانية
- الزر يصبح معطل (disabled)
- لا يمكن للمستخدم الضغط عليه

### 3. عرض عداد الوقت
```
انتظر 45 ثانية
انتظر 44 ثانية
...
انتظر 1 ثانية
```

### 4. رسائل واضحة
```
الفرنسية: "Trop de demandes. Veuillez attendre 60 secondes..."
العربية: "تم تجاوز حد الرسائل المسموح. يرجى الانتظار 60 ثانية..."
```

---

## متى يحدث Rate Limit؟

### الحالات التي تسبب Rate Limit:

1. **محاولات متكررة بنفس البريد**
   ```
   المستخدم 1: ahmed@gmail.com → محاولة الإرسال 5 مرات في دقيقة واحدة
   النتيجة: Rate Limit ❌
   ```

2. **من نفس عنوان IP**
   ```
   عنوان IP واحد → 10 طلبات بريد في دقيقة واحدة
   النتيجة: Rate Limit ❌
   ```

3. **محاولة إنشاء حسابات متعددة بسرعة**
   ```
   10 حسابات جديدة من نفس IP في دقيقة واحدة
   النتيجة: Rate Limit ❌
   ```

---

## كيف يعمل الحل الآن؟

### المسار الطبيعي (بدون أخطاء):
```
المستخدم يملأ النموذج
↓
ينقر "S'inscrire"
↓
✅ تم إرسال البريد
↓
رسالة نجاح
```

### المسار عند حدوث Rate Limit:
```
المستخدم يملأ النموذج
↓
ينقر "S'inscrire"
↓
❌ خطأ: "email rate limit exceeded"
↓
الزر معطل + عداد: "انتظر 60 ثانية"
↓
60 ثانية تمر...
↓
الزر يصبح نشط مجددا
↓
المستخدم يحاول مرة أخرى
```

---

## ملاحظات مهمة

### 1. للمستخدمين:
- إذا حصلت على رسالة "Rate Limit"، انتظر 60 ثانية
- لا تحاول الضغط عليها مرات متعددة
- الزر سيصبح نشط تلقائياً

### 2. للمطورين:
- Rate Limit يأتي من Supabase (ليس مشكلة في الكود)
- الحماية تُخفّف الضغط على الخادم
- 60 ثانية هو وقت معقول بناءً على معايير Supabase

### 3. حدود Supabase:
```
Free Plan: ~3 رسائل بريد لكل دقيقة لكل مستخدم
Pro Plan: حد أعلى من Free Plan
```

---

## اختبار الحماية

### اختبار يدوي:
```
1. افتح الموقع
2. اذهب إلى التسجيل
3. أنشئ حساب برقم بريد
4. اضغط "S'inscrire" 5 مرات بسرعة
5. يجب أن ترى: "انتظر 60 ثانية"
6. الزر معطل ✅
```

### المتوقع:
- الرسالة الأولى: تُرسل ✅
- الرسائل 2-5: معطلة ❌
- بعد 60 ثانية: يمكن المحاولة مجددا ✅

---

## الملفات المعدلة

### 1. `src/pages/Register.tsx`
```typescript
// إضافة حالة جديدة
const [rateLimitWait, setRateLimitWait] = useState(0)

// الكشف عن الخطأ
if (error.includes('rate limit') || error.includes('rate_limit') || error.includes('too many')) {
  setRateLimitWait(60)
  // عداد العد التنازلي
  const interval = setInterval(() => {
    setRateLimitWait(prev => {
      if (prev <= 1) {
        clearInterval(interval)
        return 0
      }
      return prev - 1
    })
  }, 1000)
}

// تعطيل الزر
disabled={loading || rateLimitWait > 0}

// عرض الوقت
{rateLimitWait > 0 ? `${t('auth.wait')} ${rateLimitWait}s` : t('auth.register.button')}
```

### 2. `src/lib/i18n.ts`
```typescript
// ترجمات جديدة
'auth.error.rate_limit': 'Trop de tentatives...'
'auth.error.rate_limit_wait': 'Trop de demandes...'
'auth.wait': 'Attendre'
```

---

## الخلاصة

✅ **المشكلة:** خطأ "email rate limit exceeded"  
✅ **السبب:** محاولات متعددة لإرسال بريد بسرعة  
✅ **الحل:** حماية تلقائية مع عداد العد التنازلي  
✅ **النتيجة:** تجربة مستخدم أفضل بدون حدوث الخطأ  

---

## أسئلة شائعة

### س: هل سيرى كل مستخدم هذا الخطأ؟
**ج:** لا، فقط من يحاول عدة مرات بسرعة جداً.

### س: كم وقت يجب أن ينتظر؟
**ج:** 60 ثانية (يُعدّل حسب حد Supabase).

### س: هل هذا يؤثر على الموقع؟
**ج:** لا، الحماية محلية في المتصفح فقط.

### س: ماذا لو نسى المستخدم البريد؟
**ج:** لا مشكلة، بعد 60 ثانية يمكن المحاولة مجددا.

---

## تاريخ الإضافة
- **Commit:** 1f982c7
- **Branch:** diyafa
- **التاريخ:** 2026-01-21
